rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all users for now. 
    // This should be properly secured before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
